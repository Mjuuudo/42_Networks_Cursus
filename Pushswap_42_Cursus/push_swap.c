#include "Push_swap.h"

int main(int argc, char **argv)
{
    pushswap_list *a;
    pushswap_list *b;

    a = NULL;
    b = NULL;
    if (argc <= 2  && (argv[1][0] == NULL))
            ft_putstr("Error Check The Amount Of args Passed !\n");
            return (NULL);
    else if (argc == 2)
            
    
    
}