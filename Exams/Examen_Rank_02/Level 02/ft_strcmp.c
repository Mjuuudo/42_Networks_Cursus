#include <unistd.h>


int ft_strcmp(char *s1, char *2)
{
    int counter = 0;

    while (s1[counter])
    {
        if (s1[compteur] != s2[compteur])
            return (0);
        counter++;
    }
    return (1);
}